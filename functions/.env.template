/**
 * Environment Configuration Template for Firebase Functions
 * 
 * Copy this to functions/.env and fill in your actual values
 * DO NOT commit this file to git (.gitignore already excludes it)
 */

# SendGrid API Key - Get from https://sendgrid.com/settings/api_keys
# Format: SG.xxxxxxxxxxxxx
SENDGRID_API_KEY=SG.YOUR_SENDGRID_API_KEY

# Firebase Cloud Messaging (FCM) placeholders
# Public VAPID key used by the web client to request push tokens
FCM_WEB_PUSH_PUBLIC_KEY=YOUR_FCM_WEB_PUSH_PUBLIC_KEY
# Optional: contact email for Web Push (used if you add server-side Web Push config)
FCM_WEB_PUSH_CONTACT=mailto:you@example.com
